class Solution {
    public boolean isPowerOfTwo(int n) {
        // Base case
        if (n <= 0) {
            return false;
        }
        if (n == 1) {
            return true;
        }
        // Recursive case
        return n % 2 == 0 && isPowerOfTwo(n / 2);
        
    }
}
